<div>
  <h1 class="text-3xl font-bold underline">Sourlemon Hotel</h1>
</div>
<div>
  <div>
    <% if user_signed_in? %>
      <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete } %>
      <h1 class="text-xl font bold">Welcome Home, <%= current_user.email %></h1>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path %>
    <% end %>
  </div>
  <div>
    <!-- temporary link later need to put under validation user sign in -->
    <%= link_to "Dashboard", dashboard_path %>
  </div>
</div>